//! Layout engine — port of `src/renderer/layout.ts`. Phase 8 stub.
//! Full implementation in phase 8; for scaffolding only exports a placeholder.

use crate::model::Block;

pub fn layout_block_stub(_block: &Block, _block_index: i32, _width: i32) -> Vec<String> {
    Vec::new()
}